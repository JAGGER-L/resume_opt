```
backend/
├── app/
│   ├── app.py
│   ├── config.py
│   │
│   ├── api/
│   │   ├── __init__.py
│   │   ├── routes_jobs.py
│   │   ├── routes_uploads.py
│   │   └── routes_exports.py
│   │
│   ├── graph/
│   │   ├── __init__.py
│   │   ├── state.py
│   │   ├── builder.py
│   │   ├── edges.py
│   │   └── nodes/
│   │       ├── __init__.py
│   │       ├── parse_inputs.py
│   │       ├── analyze_jd.py
│   │       ├── analyze_resume.py
│   │       ├── analyze_gap.py
│   │       ├── plan_versions.py
│   │       ├── generate_resumes.py
│   │       ├── review_resumes.py
│   │       ├── generate_interview.py
│   │       └── export_files.py
│   │
│   ├── llm/
│   │   ├── __init__.py
│   │   ├── client.py
│   │   ├── models.py
│   │   └── prompts/
│   │       ├── zh/
│   │       ├── en/
│   │       └── shared/
│   │
│   ├── schemas/
│   │   ├── __init__.py
│   │   ├── resume.py
│   │   ├── jd.py
│   │   ├── gap.py
│   │   ├── interview.py
│   │   ├── export.py
│   │   └── job.py
│   │
│   ├── services/
│   │   ├── __init__.py
│   │   ├── resume_parser.py
│   │   ├── exporter.py
│   │   ├── file_storage.py
│   │   └── job_service.py
│   │
│   ├── db/
│   │   ├── __init__.py
│   │   ├── session.py
│   │   ├── models.py
│   │   └── repositories/
│   │       ├── job_repo.py
│   │       ├── resume_repo.py
│   │       └── export_repo.py
│   │
│   └── configs/
│       └── configs.yaml
│
├── tests/
├── pyproject.toml
├── Dockerfile
└── .env.example
```

```

backend/
后端项目根目录。

backend/app/
后端应用主目录，所有业务代码放这里。

backend/app/app.py
FastAPI 入口文件，创建 app、注册路由、中间件、异常处理。

backend/app/config.py
读取环境变量，提供数据库、LLM、对象存储、运行环境等配置。

backend/app/configs/
存放非敏感配置文件。

backend/app/configs/configs.yaml
业务配置，例如默认模型、导出模板、语言配置、评分阈值。

backend/app/api/
HTTP API 层，只处理请求、响应、参数校验，不写 LangGraph 业务逻辑。

backend/app/api/routes_jobs.py
创建简历优化任务、查询任务状态、获取任务结果。

backend/app/api/routes_uploads.py
处理简历文件上传、解析输入文件地址。

backend/app/api/routes_exports.py
处理 PDF、DOCX、Markdown 下载接口。

backend/app/graph/
LangGraph 工作流核心目录。

backend/app/graph/state.py
定义 Graph State，也就是所有节点共享的结构化上下文。

backend/app/graph/builder.py
构建 LangGraph，注册节点、边、条件边、入口和结束节点。

backend/app/graph/edges.py
定义条件分支逻辑，例如评审失败是否重新生成。

backend/app/graph/nodes/
存放每个 LangGraph 节点。

backend/app/graph/nodes/parse_inputs.py
解析用户输入，包括 JD 文本、简历文本、文件内容、语言识别。

backend/app/graph/nodes/analyze_jd.py
分析 JD，提取岗位职责、必备技能、加分项、ATS 关键词。

backend/app/graph/nodes/analyze_resume.py
分析简历，提取经历、项目、技能、教育、可用事实。

backend/app/graph/nodes/analyze_gap.py
比较 JD 和简历，生成匹配点、缺失点、风险点和优化建议。

backend/app/graph/nodes/plan_versions.py
规划三版简历策略，例如 ATS 版、成果导向版、岗位匹配版。

backend/app/graph/nodes/generate_resumes.py
根据策略生成三版优化简历。

backend/app/graph/nodes/review_resumes.py
检查生成结果，防止编造，评估 ATS 覆盖率和语言质量。

backend/app/graph/nodes/generate_interview.py
根据 JD 和优化简历生成 10-15 个面试问题及回答框架。

backend/app/graph/nodes/export_files.py
触发导出服务，生成 Markdown、DOCX、PDF 文件。

backend/app/llm/
LLM 封装层，统一管理模型调用、prompt 加载、结构化输出。

backend/app/llm/client.py
创建 LLM 客户端，封装 invoke、structured output、重试等通用逻辑。

backend/app/llm/models.py
定义不同任务使用的模型，例如解析模型、生成模型、评审模型。

backend/app/llm/prompts/
存放 prompt 模板。

backend/app/llm/prompts/zh/
中文 prompt。

backend/app/llm/prompts/en/
英文 prompt。

backend/app/llm/prompts/shared/
中英文共用 prompt 片段，例如事实约束、输出格式要求。

backend/app/schemas/
Pydantic schema 目录，定义 API 输入输出和 LLM 结构化输出。

backend/app/schemas/resume.py
简历结构、简历版本、简历段落、技能结构。

backend/app/schemas/jd.py
JD 分析结果结构。

backend/app/schemas/gap.py
匹配度、缺失项、风险项、优化建议结构。

backend/app/schemas/interview.py
面试题、回答框架、考察点结构。

backend/app/schemas/export.py
导出请求、导出结果、文件格式结构。

backend/app/schemas/job.py
任务创建、任务状态、任务结果结构。

backend/app/services/
普通业务服务层，不属于 LangGraph 节点，但可被节点调用。

backend/app/services/resume_parser.py
PDF、DOCX、Markdown、TXT 简历文本提取。

backend/app/services/exporter.py
Markdown、DOCX、PDF 导出实现。

backend/app/services/file_storage.py
本地文件、S3、R2、MinIO 上传下载封装。

backend/app/services/job_service.py
创建任务、更新任务状态、保存中间结果、读取最终结果。

backend/app/db/
数据库访问层。

backend/app/db/session.py
数据库连接和 session 管理。

backend/app/db/models.py
SQLAlchemy ORM 模型。

backend/app/db/repositories/
Repository 层，封装数据库读写。

backend/app/db/repositories/job_repo.py
任务表读写。

backend/app/db/repositories/resume_repo.py
简历输入、简历版本读写。

backend/app/db/repositories/export_repo.py
导出文件记录读写。

backend/tests/
测试目录。

backend/pyproject.toml
Python 项目依赖、格式化、类型检查配置。

backend/Dockerfile
后端镜像构建文件。

backend/.env.example
环境变量示例，不放真实密钥。
```